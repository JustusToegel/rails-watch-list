<h2>List of lists:</h2>
<ul>
  <% @lists.each do |list| %>
    <li><%= cl_image_tag list.photo.key, height: 300, width: 400, crop: :fill %>
        <%= link_to list.name, list_path(list) %></li>
  <% end %>
</ul>

<p><%= link_to "create new list", new_list_path %></p>
